@baseUrl = http://localhost:5083
@token = {{login.response.body.token}}

### 1. Register New User with ASP.NET Core Identity
POST {{baseUrl}}/api/auth/register
Content-Type: application/json

{
  "username": "newplayer",
  "email": "newplayer@game.com",
  "password": "Player123!"
}

### 2. Login with Identity (testuser)
# @name login
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "Password123!"
}

### 3. Login with player1
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "player1",
  "password": "Player123!"
}

### 4. Get My Coins (Protected - Requires Auth)
GET {{baseUrl}}/api/auth/coins
Authorization: Bearer {{token}}

### 5. Get My Profile (Protected)
GET {{baseUrl}}/api/auth/profile
Authorization: Bearer {{token}}

### 6. Change Password (Protected)
POST {{baseUrl}}/api/auth/change-password
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "currentPassword": "Password123!",
  "newPassword": "NewPassword123!"
}

### 7. Logout (Protected)
POST {{baseUrl}}/api/auth/logout
Authorization: Bearer {{token}}

### 8. Test Invalid Login (Should fail)
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "WrongPassword"
}

### 9. Test Weak Password (Should fail validation)
POST {{baseUrl}}/api/auth/register
Content-Type: application/json

{
  "username": "weakuser",
  "email": "weak@game.com",
  "password": "123"
}
